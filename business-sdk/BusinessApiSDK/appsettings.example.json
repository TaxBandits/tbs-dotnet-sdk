{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "AppSettings": {
    //Begin JWT
    "OAUTH_API_URL": "--OAUTH URL--",
    "TBS_PUBLIC_API_BASE_URL": "--API URL",

    //JWT Credentials from Dev Console https://sandbox.taxbandits.com/ (Settings ==> API Credentials)
    "ClientId": "--Your ClientId--",
    "ClientSecret": "--Your ClientSecret--",
    "UserToken": "--Your UserToken--"

    //End JWT
  }
}
